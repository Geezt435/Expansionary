---
import { getIndex } from "@lib/contentParser";
import HomeSingle from "@layouts/HomeSingle.astro";
import type { HomeEntry } from "@/types";

const entry = (await getIndex("home")) as HomeEntry;
---

<HomeSingle entry={entry} />
