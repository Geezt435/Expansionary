---
import type { AboutEntry } from "@/types";
import PageLayout from "@components/about/PageLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("about")) as AboutEntry;
---

<PageLayout entry={entry} />
