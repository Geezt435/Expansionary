---
import AboutSingle from '@layouts/AboutSingle.astro';
import { getEntry, render } from 'astro:content';

const about = await getEntry("about", "-index");
const { Content } = await render(about);
---

<AboutSingle
  data={about.data}
>
  <Content />
</AboutSingle>
