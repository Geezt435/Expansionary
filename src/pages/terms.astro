---
import TermsSingle from "@layouts/TermsSingle.astro";
import { getEntry, render } from 'astro:content';

const terms = await getEntry("terms", "-index");
const { Content } = await render(terms);
---

<TermsSingle data={terms.data}>
  <Content />
</TermsSingle>
